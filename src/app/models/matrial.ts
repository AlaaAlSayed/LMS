import { FILE } from "dns";
import { fileURLToPath } from "url";
import { FILE } from "dns";
export class matrial{
    id:number=0 ;
    subjectId:number=0;
    name:string='';

     input_file :FormData=new FormData();

}